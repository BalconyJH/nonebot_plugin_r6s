<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Player Profile</title>
    <style>
        body {
            font-family: {{ font_family }}, sans-serif;
            background-color: #121212;
            color: #f5f5f5;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .stat-text-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 5px 5px;
        }

        .profile-container {
            display: flex;
            background-color: #1f1f1f;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
            width: 1200px;
            height: 600px;
            padding: 20px;
        }

        .left-container,
        .right-container {
            width: 50%;
            padding: 20px;
        }

        .profile-image {
            background-color: #6d6d6d;
            border-radius: 10px;
            width: 100%;
            aspect-ratio: 1 / 1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .profile-image .avatar {
            border-radius: 10px;
            background-color: #333;
            font-size: 24px;
            color: #555;
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .profile-details h1 {
            margin-top: 0;
            font-size: 30px;
            margin-bottom: 20px;
        }

        .name-level-container {
            display: flex;
            align-items: baseline;
            gap: 20px;
        }

        .level {
            margin-top: 0;
            background-color: #444;
            padding: 5px 10px;
            border-radius: 10px;
            color: #fff;
            font-size: 14px;
        }

        .rank-container {
            display: flex;
            gap: 20px;
            margin-bottom: 10px;
        }

        .text-line {
            font-size: 14px;
            color: #ccc;
            margin: 5px 0;
        }

        .text-line.bold {
            font-weight: bold;
        }

        .additional-info {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            background-color: #333;
            padding: 10px;
            border-radius: 10px;
        }

        .rank-container .rank {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            font-size: 14px;
        }

        .rank-container .rank-icon {
            background-color: #444;
            color: #fff;
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 8px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 18px;
            width: 50px;
            height: 50px;
        }

        .stats-chart-container {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 10px;
        }

        .stats {
            background-color: #333;
            padding: 10px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            position: relative;
        }

        .stats-content {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            margin-left: 15px;
        }

        .stat-item {
            background-color: #444;
            padding: 5px 10px;
            border-radius: 10px;
            color: #fff;
            font-size: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 150px;
            margin-bottom: 5px;
            margin-top: 5px;
            text-align: center;
        }

        .stat-text {
            color: #ccc;
            font-size: 12px;
            text-align: right;
            width: 100%;
            margin: 2px 0;
        }


        #main {
            width: 250px;
            height: 125px;
        }

        .roles-container {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .roles-container .role-section {
            width: 48%;
        }

        .section-title {
            font-weight: bold;
            margin-bottom: 10px;
        }

        .section-title.version {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            background-color: #333;
            padding: 10px;
            border-radius: 10px;
        }

        .role-icons {
            display: flex;
            gap: 15px;
        }

        .role-icons .icon {
            background-color: #444;
            border-radius: 10px;
            width: 50px;
            height: 50px;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #ccc;
            font-size: 18px;
        }

        .aliases {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .aliases .alias {
            background-color: #444;
            padding: 5px 10px;
            border-radius: 10px;
            color: #fff;
            font-size: 14px;
        }
    </style>
</head>
<body>

<div class="profile-container">
    <div class="left-container">
        <div class="profile-image">
            <img class="avatar" src="{{ avatar_url }}" alt="Avatar">
        </div>
    </div>
    <div class="right-container">
        <div class="name-level-container">
            <h1>{{ player_name }}</h1>
            <div class="level">Level: {{ player_level }}</div>
            <div class="level">MMR: {{ player_mmr }}</div>
        </div>
        <div class="section-title">Rank</div>
        <div class="rank-container">
            {% for rank in ranks %}
                <div class="rank">
                    <img class="rank-icon" src="{{ rank.icon_url }}"
                         alt="{{ rank.name }} Icon">
                    <div>{{ rank.name }}</div>
                </div>
            {% endfor %}
            <div class="additional-info">
                <div class="text-line bold">Powered by</div>
                <div class="text-line bold">Nonebot & nonebot-plugin-r6s</div>
                <div class="text-line">Generation time: {{ generation_time }}</div>
            </div>
        </div>
        <div class="section-title">Info</div>
        <div class="stats-chart-container">
            <div class="stats">
                <div class="stat-text-container">
                    <div class="stat-text">Rank</div>
                </div>
                <div class="stats-content">
                    <div class="stat-item">KD: {{ rank_kd }}</div>
                    <div class="stat-item">KPM: {{ rank_kpm }}</div>
                    <div class="stat-item">WinRate: {{ rank_wr }}</div>
                </div>
            </div>
            <div class="stats">
                <div class="stat-text-container">
                    <div class="stat-text">Quick</div>
                    <div class="stat-text">Match</div>
                </div>
                <div class="stats-content">
                    <div class="stat-item">KD: {{ quick_kd }}</div>
                    <div class="stat-item">KPM: {{ quick_kpm }}</div>
                    <div class="stat-item">WinRate: {{ quick_wr }}</div>
                </div>
            </div>
        </div>
        <div class="roles-container">
            <div class="role-section">
                <div class="section-title">Attack</div>
                <div class="role-icons">
                    {% for attack_role in attack_roles %}
                        <img class="icon" src="{{ attack_role.icon_url }}"
                             alt="{{ attack_role.name }} Icon">
                    {% endfor %}
                </div>
            </div>
            <div class="role-section">
                <div class="section-title">Defense</div>
                <div class="role-icons">
                    {% for defense_role in defense_roles %}
                        <img class="icon" src="{{ defense_role.icon_url }}"
                             alt="{{ defense_role.name }} Icon">
                    {% endfor %}
                </div>
            </div>
        </div>
        <div class="section-title version">🚧Insider preview version: 0.5.0🚧</div>
        {#        <div class="section-title">Aliases</div>#}
        {#        <div class="aliases">#}
        {#            {% for alias in aliases %}#}
        {#                <div class="alias">{{ alias }}</div>#}
        {#            {% endfor %}#}
        {#        </div>#}
        {#        <div class="section-title">Weapons</div>#}
        {#        <div class="aliases">#}
        {#            {% for alias in aliases %}#}
        {#                <div class="alias">{{ alias }}</div>#}
        {#            {% endfor %}#}
        {#        </div>#}
    </div>
</div>

</body>
</html>